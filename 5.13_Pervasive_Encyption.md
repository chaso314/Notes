# 5.13 広範囲の暗号化 Pervasive Encryption
## Pervasive Encryptionとは?
- 転送中(In-Flight)及び保管中(At-Rest)のデータを透過的(Transparent)かつ包括的(Pervasive)に保護する暗号化の仕組み。
### 透過性(Transparent)
- 暗号化にあたってアプリケーション修正や定義変更が不要なので、データ保護やコンプライアンス要件遵守のためのコストやプロセスを削減可能。ハードウェア、OS、ミドルウェアで同じように利用可能。
### 広範囲(Pervasive)
- 転送中(In-Flight)だけでなく、保管中(At-Rest)も含め全データが暗号化対象
### 暗号化
- プロセッサ(CP)内蔵の暗号化支援専用CP(CPACF: Central Processor Assist for Cryptographic Functions)で暗号化に伴うCPU負荷を軽減。専用CPで高速に暗号化
- CPACFの利用によりバルクファイルのような大量レコードも、全データを暗号化対象として暗号化が可能

> CPACF(Central Processor Assist for Cryptographic Functions): 暗号化処理をハードウェアレベルで提供するプロセッサーの種類の1つ。ICSFとCrypto Express cardによる暗号化処理よりも高速。CPACFではClear Key(メモリ上で暗号化されずダンプに露出する鍵)とProtected Key(ラップされCPACF内でのみ復号される鍵)が使用される。CPACF内ではClear Keyを用いた暗号化と復号化のための共通鍵暗号処理が実施される

> 暗号化支援専用CPを使用せず普通のCPを用いた暗号化では大量データの暗号化に時間がかかるため、重要データのみを識別/分類してから暗号化するなどしており、未分類/誤分類データの暗号化未済がリスクとなっていた。システム上の全データを暗号化できるのでリスクを低減可能

## 5.13.1 z/OSの暗号化機能 z/OS Encryption Capabilities
z14ではPervasive Encryptionといったポリシーに沿った暗号化機能を提供するよう設計されており、以下の新機能が含まれる。
### z14の新機能
- 広範なデータセット保護: アプリケーションにデータ暗号化APIを組み込むための変更を行わずに、データセット、zFSファイルシステム、CPに対するデータ保護を提供
- ポリシーによる制御(z/OS policy controls): Pervasive Encryptionの利用により、データ保護やコンプライアンス要件遵守のタスクを簡素化
- 暗号化ポリシー準拠対応: z/OS Communications Serverには号化準備テクノロジー（encryption-readiness technology）が含まれている。z/OSシステムへのTCPおよびEnterprise Extenderのトラフィックパターンが暗号化ポリシーを満たしているか判断しやすく、コンプライアンス対応が容易になる。
### z/OSのデータセット暗号化
z/OSではデータセット暗号化は高速で低コスト。アプリケーションを変更せずに利用できるのでアプリケーションによらずに暗号化機能を利用可能(透過的)。データセットはAES256で暗号化される
### z/OSのデータセット暗号化の利点
- 高セキュリティと高速暗号化: CPACFとProtected Keyの利用により、鍵が露出しない。高セキュリティと高速暗号化を両立
- 既存のアクセス制御との整合性を担保
- 高速かつ効率的な実行
- アプリケーションの変更が不要: アプリケーションやデータベースを変更せずに暗号化機能を有効化可能
- 暗号化の持続: データの複製/バックアップ/統合後も暗号化を維持
- メモリ内バッファの非暗号化: メモリのバッファ内容を暗号化しないことで、データアクセスの度に暗号化/復号が必要ない
- 暗号鍵の論理環境での分離: 論理環境毎に暗号鍵を所有/管理可能
- コンプライアンス対応を簡素化

## 5.13.2 z/OS DFSMS and pervasive encryption
