# 5.7 共通仮想ストレージ領域 (The common virtual storage area)
## はじめに、z/OSの仮想ストレージの仕組み
- プログラムが利用する個々の仮想ストレージをアイソレーション(Isolation)するため、z/OSでは1つのプログラムに1つのアドレス空間を1つの仮想ストレージとして割り当てる。
> アイソレーション/隔離(Isolation): 他のアドレス空間からの読み書きを禁止すること

>[!TIP]
> 仮想ストレージを使うことで、アドレス空間内でのエラーが他のアドレス空間に影響せず高セキュリティ

>[!CAUTION]
>異なるプログラムではアドレス空間が異なるためアドレス空間をまたぐデータのやり取り/通信が必要

## 共通仮想ストレージとは?
- 異なるプログラムで通信/データアクセスするために仮想ストレージの一部を共通領域(common area)として割り当てたメモリ領域。他のアドレス空間からも同じ仮想アドレスでアクセスできる領域。仮想ストレージはアイソレーションが大原則だが例外的に一部を共有領域にすることでセキュリティを担保したまま利便性を向上させる。
- すべてのアドレス空間が共通領域をもつ
> 対義語: プライベート領域(後述)

![Fig 5-10](https://github.com/user-attachments/assets/757e2be8-d21e-4a29-9304-e97ceac8d444)
> 画像は31bitシステムでの仮想ストレージ例

## 共通領域(common area)の種別
### PSA (Prefixed Storage Area: プリフィックス保存領域)
- 各CP固有のアドレス値を格納する仮想アドレス領域。1つのCPが1つのPSAをもつ。CPが変わるとPrefixing機能で異なる物理メモリを参照する様に仮想アドレス0番地が変換される(DAT: 動的アドレス変換)。
- 仮想ストレージのPSAのみExtended領域(16MB以上の領域)には配置されない
- 仮想ストレージ内のアドレス0～8191番地(8KB分)がPSAとして使用される
> CP固有の情報(実行中タスクアドレス、状態など)はCP毎に異なり、これをCP毎に保存するためにPSAは用いられる。プロセスへの複数CP割り当てを実施した際、CP0番の情報は物理メモリA、CP1番の情報は物理メモリBなどと参照すべき物理メモリやアドレスは異なる状態となる。CPが変わる際にPrefixing機能が仮想アドレス0番地を変更(Prefixレジスタ値の加算または置換)してくれるので、OSは常に各CP用の物理メモリを同じベースアドレス指定方式命令(0番地からのオフセットを指定するアドレス指定命令(L R1, PSATOLD (レジスタR1のオフセット値で実行中タスクアドレスを取得)など)でCP固有の情報を取得できるようになる。Linuxなどと異なりアドレス空間切替のためDATでなくCP(CPU)切替のためのDATがアドレス0番地を見る設計のマルチプロセッサホストでは必要であり、そのための固有領域8KBがPSA)

### CSA (Common Service Area: 共通サービス領域)
- すべてのアドレス空間からアドレス可能な共通領域。IPL時にサイズが決定される(稼働中にサイズ変更できない)。
- JES2、DFSMS(Data Facility Storage Management Subsystem)、RACF(Resource Access Control Facility)、VSAM(Virtual Storage Access Method)などが使用する制御ブロック(ontrol Block)などが置かれる
- CSAはLPA直下に、ECSA(Extended CSA)はMLPA直上に割り当てられる
- SQA/ESQA不足時は、z/OSによりCSA/ECSAから領域を確保し、SQA/ESQAとして割り当てる
> 制御ブロック(Control Block): OSがリソースの状態やプログラムの実行情報を記録するために使用するデータ構造。

### LPA (PLPA, FLPA, MLPA: リンク・パック領域)
- 再入可能(Reentrant)な読み取り専用プログラムモジュール(SVC(スーパバイザコール)、デバイスアクセス、システム共有など)をLoadするための領域。ページングの可不可や修正可不可によらず、本領域にLoadされる。共通プログラムを1か所に配置することで実メモリの消費を抑えられる
- PLPA(Pageable Link Pack Are): ページング可能。通常、プログラムが格納される領域。ページング可能なので、実メモリ（CPCメモリ）が不足した際、補助記憶装置（DASD）へ退避（ページアウト）させる
- FLPA(Fixed LPA): ページ固定(非ページング)。パフォーマンス向上のためにメモリ固定したいプログラムを置くときに使用される領域。
- MLPA(Modified LPA): 修正可能。システム稼働中にPLPAを修正・拡張するために一時的に使用される領域
> 再入可能(Reentrant): 複数のタスクが同時に実行しても、コードを書き換えず正しく動作すること

### SQA (System Queue Area: システム・キュー領域)
- システム管理に使うキューやテーブルや制御ブロックなどのデータを置く領域。ページング不可。プログラムで使用中は実メモリ(CPCメモリ)に常駐し使用後freeされる。IPL時にサイズが決定される(IPL時にメモリが割り当てられる)。
- 実行中プログラム(Workload)が増えるほど、OSがプログラムを管理するために使用するSQAも増える。

### Nucleus (ニュークリアス: カーネル(中核領域)) 
- Nucleusを格納する領域。読み取り専用でページング不可かつスワップ不可。IPL時に実メモリにLoadされ常駐する。
- RMODE(Residency Mode: 仮想ストレージの場所指定属性値)によりNucluesプログラムの置き場所が異なる。
  - RMODE24: 24ビットアドレッシングの範囲内(16MBより下)にロード。
  - RMODE31: 31ビットアドレッシングの範囲内(16MBより上)にロード可能。
> Nucleus: OSのkernel(中核)になるプログラムのこと。ソフトウェアとハードウェアのつなぎ役としてリソースの管理と制御を行う(プロセス管理、メモリ管理、デバイス制御、システムコールなど)。IPL時に実メモリにLoadされ常駐する。

![Fig 5-11](https://github.com/user-attachments/assets/c0402931-c335-45b5-86a2-359066c34170)
## 5.7.6 アドレススペース・プライベート領域 (Address space private area)
### アドレススペースにおけるプライベート領域とは?
- 各アドレススペースに割り当てられた仮想ストレージのうち、そのジョブやユーザーが占有（排他利用）できる領域
- 仮想ストレージサイズから共通仮想ストレージサイズを引いた残りがプライベート領域サイズとなる。プライベート領域内の仮想アドレスは、その領域内で実行されるプログラムに対してユニーク。
- PVT (Private): 16MBラインより下の領域。
- EPVT (Extended Private): 16MBラインより上の領域。

>[!TIP]
> z/OSの`common virtual storage area（共通仮想ストレージ領域）`はプライベート領域(プログラムユニーク領域)以外の仮想ストレージ領域。すべてのアドレス空間から共通の仮想アドレスでアクセスできるメモリ領域。仮想ストレージのアイソレーションによるセキュリティを担保したままメモリアクセス性と再利用性を向上させる機能
